import { useMemo } from 'react';

const useChatStyles = (mode) => {
    return useMemo(() => {
        const styles = {

        };

        return styles;
    }, [mode]);
};

export default useChatStyles;
