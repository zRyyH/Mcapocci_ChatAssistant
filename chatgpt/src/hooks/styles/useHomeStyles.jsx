import { useMemo } from 'react';

const useHomeStyles = (mode) => {
    return useMemo(() => {
        const styles = {

        };

        return styles;
    }, [mode]);
};

export default useHomeStyles;
